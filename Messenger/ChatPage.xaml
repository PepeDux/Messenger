<Page x:Class="Messenger.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Messenger"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600"
      Title="ChatPage">

    <Grid>
        <TextBox x:Name="Chat" IsReadOnly="True" Margin="40,40,40,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="350" FontSize="20" AcceptsTab="True" AcceptsReturn="True"/>
        <TextBox x:Name="Message" Margin="40,0,40,40" TextWrapping="Wrap" Height="50" FontSize="20" VerticalAlignment="Bottom" KeyDown="Message_KeyDown"/>
    </Grid>
</Page>
